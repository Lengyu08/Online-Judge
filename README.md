### 环境准备

```bash
create-react-app oj-fe
npm i redux react-redux @reduxjs/toolkit # react 树
npm i react-router-dom # 路由组件
npm i bootstrap # 响应式布局
npm install katex @matejmazur/react-katex # react-katex
npm install --save video-react # react 视频模块
npm install --save chart.js # chart
```